az account set -s 'vs_sandbox'
cd /mnt/c/Users/jtudong/code/pan_firewall_dev_lab

az group delete -n sfc-network-dev-rg__ --yes
az group delete -n sfc-firewall-dev-rg__ --yes

az group delete -n sfc-firewall-dev-rg__ --yes&&az group delete -n sfc-network-dev-rg__ --yes

az deployment sub validate -l westus3 -f main.json -p main.parameters.json
az deployment sub create -c -l westus3 -f main.json -p main.parameters.json

git add -A;git commit